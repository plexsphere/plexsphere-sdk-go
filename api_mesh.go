/*
plexsphere API

HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/_*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/_*`) is exempt because it cannot carry a Principal yet.

API version: v1
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package plexsphere

import (
	"bytes"
	"context"
	"io"
	"net/http"
	"net/url"
	"os"
	"strings"
	"time"
)

type MeshAPI interface {

	/*
		DeleteNodeStateReport Delete an upstream node-state report entry for a Node.

		Deletes the upstream node-state report entry the addressed Node recorded under the addressed key. Deleting a report emits NO `node_state_updated` event. A delete for a key the Node carries no report under is answered with 404.  The caller authenticates with its NSK in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only delete its own reports.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the report-delete scope.
		@param key Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).
		@return ApiDeleteNodeStateReportRequest
	*/
	DeleteNodeStateReport(ctx context.Context, id string, key string) ApiDeleteNodeStateReportRequest

	// DeleteNodeStateReportExecute executes the request
	DeleteNodeStateReportExecute(r ApiDeleteNodeStateReportRequest) (*http.Response, error)

	/*
		FetchNodeSecret Fetch a Secret Store entry rewrapped under the calling Node's NSK.

		Returns the named Secret Store entry for the addressed Node as an AES-256-GCM ciphertext envelope rewrapped under the calling Node's Node Secret Key (NSK). The Secret Store is the platform's only meet-and-rewrap point between the OpenBao backend and the per-Node NSK: the handler reads the OpenBao plaintext, recovers the NSK, and rewraps the payload in-process so that ONLY NSK-wrapped ciphertext ever crosses the wire. The transient OpenBao plaintext and the recovered NSK are zeroed on every exit path; no plaintext is ever persisted, logged, cached, or placed on the event bus.  The 200 body is the raw envelope `<12-byte nonce> || <ciphertext + 16-byte GCM tag>`, served as `application/octet-stream`. The caller recovers the seeded plaintext byte-for-byte with `AES-256-GCM-Open` under its NSK. The response carries the served version in `X-Plexsphere-Secret-Version`, the NSK key id used for the wrap in `X-Plexsphere-Secret-KID` (so plexd can pick the right NSK during a rotation overlap), and `Cache-Control: no-store` so no intermediary ever retains the ciphertext.  The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401. Only a      Node proving possession of its NSK can reach the rewrap      pipeline.   2. Resolves the addressed secret metadata, applies the      per-Node and per-Domain rate limits, and runs the      `node-agent` ReBAC visibility check on the addressed Node.      An authenticated caller without the relation receives 403      `PermissionDenied`; the denial is recorded with      `insufficient_relation`.   3. Reads the entry from the OpenBao backend — the current      version when `version` is omitted, or the requested version      when present — reconciling the served version against the      metadata; the OpenBao-returned version wins for the header.   4. Rewraps the payload under the recovered NSK with a fresh      12-byte nonce and emits exactly one granted audit row. A      backend read that fails AFTER a granted authorization still      records the granted audit row — operational outcomes are      carried on the fetch-duration metric, never on the audit      chain.  The response body is capped at 1 MiB of ciphertext.  DEFERRED-WIRING POSTURE: when the Secret Store backend is not configured (empty backend DSN) the production composition root leaves this surface disabled and every request returns 501 with `code: secrets_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the secret-fetch scope.
		@param name Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters).
		@return ApiFetchNodeSecretRequest
	*/
	FetchNodeSecret(ctx context.Context, id string, name string) ApiFetchNodeSecretRequest

	// FetchNodeSecretExecute executes the request
	//  @return *os.File
	FetchNodeSecretExecute(r ApiFetchNodeSecretRequest) (*os.File, *http.Response, error)

	/*
		GetDomainMeshTopology Return the mesh topology for a Domain.

		Returns the read-side projection of the mesh fabric that backs the dashboard mesh-map view and the `plexctl mesh topology` CLI: every live Peer Node anchored to the Domain together with the directed edges between them. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the `domain-view` ReBAC relation on the addressed      Domain BEFORE any existence check so the endpoint cannot      be used as a Domain-id oracle.   3. Resolves the per-Domain peer graph from the SSE peer-      delta projection — `nodes` carries each anchored Node's      mesh-IP and reachability, `edges` carries each pairwise      relationship with its `mode` (`direct` or `relayed`), the      handshake age, and the bridge Node currently serving as      the relay fallback when one is assigned.  The payload mirrors the SSE peer-graph events one-for-one so a renderer that consumes the live stream and a renderer that polls this pull converge on byte-identical state.  DEFERRED-WIRING POSTURE: until the production composition root supplies the mesh-topology projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: mesh_topology_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param domainId Domain identifier (UUIDv7) — the topology scope.
		@return ApiGetDomainMeshTopologyRequest
	*/
	GetDomainMeshTopology(ctx context.Context, domainId string) ApiGetDomainMeshTopologyRequest

	// GetDomainMeshTopologyExecute executes the request
	//  @return MeshTopology
	GetDomainMeshTopologyExecute(r ApiGetDomainMeshTopologyRequest) (*MeshTopology, *http.Response, error)

	/*
		GetNodeEvents Stream signed envelope events for a Node over SSE.

		Opens a Server-Sent Events stream of canonical signed `Envelope` records scoped to the addressed Node. Each `data:` frame is the JSON wire body produced by `internal/signing/envelope.CanonicalBytes`. The server runs ed25519 verification against the per-Domain signing public key before emitting the frame; consumers SHOULD verify the trailing `signature:` field again as a defence-in-depth measure.  The `id:` field on every event frame is the JetStream stream sequence number for that envelope. Clients resume by re- connecting with `Last-Event-ID: <numeric>` set to the last sequence they durably processed; the server replays from the next sequence (`> last`). When the header is absent or empty the stream tails from now — historical events are NOT backfilled.  The server emits a 25-second SSE comment-frame keep-alive (`:keep-alive\\n\\n`) so idle proxies do not collapse the connection. The `X-Plexsphere-API-Version` response header carries the contract version the stream conforms to; `Cache-Control: no-cache` opts the response out of any intermediary caching layer.  DEFERRED-WIRING POSTURE: the production composition root does not yet supply the SignatureVerifier, RelationChecker, or NodeRepo ports the handler depends on. Until those ports are wired, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned`. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the SSE stream scope.
		@return ApiGetNodeEventsRequest
	*/
	GetNodeEvents(ctx context.Context, id string) ApiGetNodeEventsRequest

	// GetNodeEventsExecute executes the request
	//  @return string
	GetNodeEventsExecute(r ApiGetNodeEventsRequest) (string, *http.Response, error)

	/*
		GetNodeKeysRotatePreview Preview the fleet impact of rotating a Node's mesh key.

		Returns a non-mutating dry-run of the fleet impact a manual mesh-key rotation against the addressed Node would have. Backs the Phase-2 dashboard \"manual key-rotation with impact preview\" view and the `plexctl key rotate --preview` CLI. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the same operator-action ReBAC relation      `node-operate` on `node:<id>` that the mutating      `POST /v1/nodes/{id}/keys/rotate` checks, BEFORE any      existence check, so an unauthorised caller cannot probe      Node existence via the 404/403 timing differential. A      caller lacking the relation receives 403      `insufficient_relation` and an audit row with outcome      `insufficient_relation`.   3. Resolves the per-Node read-side projection — the live      Peer the rotation would target, every peer edge whose      PSK would be re-issued, and the in-flight rotation row      (if any). NO `peer_key_rotation` row is created, NO      `rotate_keys` outbox event is appended, and NO audit row      beyond the read-side ReBAC decision is emitted.  The 200 response payload mirrors the mutating endpoint's view of the world one-for-one — `peer_id` matches what the live `POST` would return, `already_pending` flips on a pre-existing in-flight rotation the same way — so a renderer that consumes both surfaces can drive the dashboard confirmation dialog from a single mapping.  DEFERRED-WIRING POSTURE: until the production composition root supplies the read-side preview projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_preview_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the rotation scope.
		@return ApiGetNodeKeysRotatePreviewRequest
	*/
	GetNodeKeysRotatePreview(ctx context.Context, id string) ApiGetNodeKeysRotatePreviewRequest

	// GetNodeKeysRotatePreviewExecute executes the request
	//  @return RotationImpactPreview
	GetNodeKeysRotatePreviewExecute(r ApiGetNodeKeysRotatePreviewRequest) (*RotationImpactPreview, *http.Response, error)

	/*
		GetNodeReachability Read the reachability projection for a Node.

		Returns the latest `Reachability` projection for the addressed Node — the per-Node state-machine the heartbeat handler advances on every accepted heartbeat. The projection is the authoritative health view the operator UI and reconciliation surface consume; it transitions `healthy` → `stale` after 90s without a heartbeat and `stale` → `unreachable` after 300s, with `changed_at` tracking the most recent transition.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/state` and `GET /v1/nodes/{id}/events`, so any caller authorised to issue a reconciliation pull is also authorised to read the reachability projection. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle .  DEFERRED-WIRING POSTURE: until the production composition root supplies the ReachabilityRepo, RelationChecker, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: reachability_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the reachability scope.
		@return ApiGetNodeReachabilityRequest
	*/
	GetNodeReachability(ctx context.Context, id string) ApiGetNodeReachabilityRequest

	// GetNodeReachabilityExecute executes the request
	//  @return Reachability
	GetNodeReachabilityExecute(r ApiGetNodeReachabilityRequest) (*Reachability, *http.Response, error)

	/*
		GetNodeState Reconciliation pull for a Node — return the canonical NodeStateSnapshot.

		Returns the canonical `NodeStateSnapshot` for the addressed Node. The snapshot is the authoritative cold-start view that plexd consumes when it first comes up, when its SSE connection has been disconnected for longer than the replay window, or when an out-of-band request arrives to re-derive the desired state. The four wire blocks — `peers`, `policy`, `bridge`, and `state`/`reports` — are always present so plexd's reconcile loop can diff by field presence rather than absence; later stories populate the currently-empty blocks without changing the wire shape .  The peer projection is a single SQL round-trip ordered by `node_id ASC` so two consecutive pulls against the same ledger snapshot are byte-equal — plexd's reconcile loop reduces redundant rewrites by hashing the response.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/events`, so any caller authorised to subscribe to a Node's SSE event stream is also authorised to issue a reconciliation pull. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle.  DEFERRED-WIRING POSTURE: until the production composition root supplies the `SnapshotProvider`, `RelationChecker`, and `NodeRepo` ports the handler depends on, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the snapshot scope.
		@return ApiGetNodeStateRequest
	*/
	GetNodeState(ctx context.Context, id string) ApiGetNodeStateRequest

	// GetNodeStateExecute executes the request
	//  @return NodeStateSnapshot
	GetNodeStateExecute(r ApiGetNodeStateRequest) (*NodeStateSnapshot, *http.Response, error)

	/*
		PostKeysRotate Submit a freshly-generated Curve25519 key to complete a rotation.

		Completes a pending mesh-key rotation for the calling Node. After an operator triggers a rotation via `POST /v1/nodes/{id}/keys/rotate` — or the heartbeat response sets `rotate_keys: true` — plexd generates a fresh Curve25519 keypair and submits the new public key here. The handler:    1. Authenticates the caller against the Node Secret Key      (NSK) plaintext supplied in the `Authorization: Bearer`      header. The route carries NO path `{id}` segment — the      rotating Node is identified solely by the NSK envelope it      presents — so a missing or unresolved NSK surfaces as      401.   2. Caps the request body at 4 KiB; oversize bodies receive      413 `keys_rotate_body_too_large` without ever touching      the JSON decoder.   3. Validates the submitted `new_public_key` — it MUST decode      to exactly 32 bytes and MUST NOT be the all-zero      degenerate value — BEFORE any database read (otherwise      422 `keys_rotate_public_key_invalid`).   4. Resolves the caller's live Peer row; a Node with no live      Peer surfaces as 404 `keys_rotate_peer_not_found`.   5. Hands the new key to the rotation completer, which — in      one transaction — updates `nodes.public_key`, retires the      old pairwise PSK, wraps and inserts a fresh PSK, appends      a `peer_key_rotated` outbox event, and flips the pending      `peer_key_rotation` row to `completed`.  The 200 response carries the completed rotation's id plus the `(kid, wrap_key_version)` reference of the re-issued PSK — it never carries PSK plaintext or ciphertext. An idempotent retry with the same key against an already-completed rotation returns the prior receipt and emits no second event.  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationCompleter, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@return ApiPostKeysRotateRequest
	*/
	PostKeysRotate(ctx context.Context) ApiPostKeysRotateRequest

	// PostKeysRotateExecute executes the request
	//  @return KeysRotateResponse
	PostKeysRotateExecute(r ApiPostKeysRotateRequest) (*KeysRotateResponse, *http.Response, error)

	/*
		PostNodeAudit Ingest a batch of normalised audit events for a Node.

		Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `AuditEvent` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki with a separate retention class and a distinct SIEM route. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `source` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: audit-ingest labels carry Domain, Project, and Node — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the ingest scope.
		@return ApiPostNodeAuditRequest
	*/
	PostNodeAudit(ctx context.Context, id string) ApiPostNodeAuditRequest

	// PostNodeAuditExecute executes the request
	//  @return IngestReceipt
	PostNodeAuditExecute(r ApiPostNodeAuditRequest) (*IngestReceipt, *http.Response, error)

	/*
		PostNodeHeartbeat Record a Node liveness heartbeat and return reconcile/rotate hints.

		Accepts a per-Node liveness heartbeat from plexd and updates the reachability projection that drives the mesh-wide health view . The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Validates the request body — `client_now` MUST be within      60s of server now (otherwise 400 `clock_skew`),      `binary_checksum` MUST be present and decode to a 32-byte      SHA-256 digest (otherwise 400 `binary_checksum_empty`)      .   4. Persists the heartbeat fact and updates the per-Node      reachability state-machine (`healthy` → `stale` after 90s,      `stale` → `unreachable` after 300s) so the projection at      `GET /v1/nodes/{id}/reachability` reflects the new fact      on the next read.  The 200 response carries `accepted_at` (server timestamp at commit) and two reconciliation flags. `reconcile` defaults to `false` and later stories flip it when the controller wants plexd to issue a fresh reconciliation pull. `rotate_keys` is load-bearing: it is set to `true` whenever a `peer_key_rotation` row is pending for the heartbeating Node, telling the caller to generate a fresh Curve25519 keypair and complete the rotation via `POST /v1/keys/rotate`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the NSK validator, ReachabilityRepo, and clock-skew evaluator the handler depends on, every request to this endpoint returns 501 with `code: heartbeat_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the heartbeat scope.
		@return ApiPostNodeHeartbeatRequest
	*/
	PostNodeHeartbeat(ctx context.Context, id string) ApiPostNodeHeartbeatRequest

	// PostNodeHeartbeatExecute executes the request
	//  @return HeartbeatResponse
	PostNodeHeartbeatExecute(r ApiPostNodeHeartbeatRequest) (*HeartbeatResponse, *http.Response, error)

	/*
		PostNodeIntegrityViolations Ingest a batch of integrity-violation reports for a Node.

		Accepts a batch of `IntegrityViolation` reports from plexd describing tamper-evidence divergences the agent detected on the local Node: a mismatched binary checksum, a tampered Lua hook checksum, or a rotated SSH host-key fingerprint. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `integrity_violations_body_too_large` without ever      touching the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_integrity_violations_request`.   5. Canonicalises every entry through the      `tenancy.NewIntegrityViolation` value-object constructor      which enforces every per-violation invariant: `kind` in      the closed set `{binary_checksum, hook_checksum,      ssh_host_key}`, `detected_by` in the closed set      `{startup_scan, inotify, pre_dispatch}`, `artifact_id`      non-empty after trimming, `observed_checksum`/      `expected_checksum` exactly 32 bytes for the checksum      kinds, `observed_fingerprint`/`expected_fingerprint`      matching `SHA256:<base64>` for the `ssh_host_key` kind,      and the per-kind column-mismatch guard that rejects a      checksum kind carrying fingerprint fields and vice versa.   6. Hands the canonical batch to the Node aggregate's      `RecordIntegrityViolations` method which enforces the      batch-level invariants: at least one entry      (`integrity_violations_empty`) and at most 128 entries      (`integrity_violations_too_many`).   7. Persists every violation row and appends a single      `integrity_alert` outbox event inside one transaction so      the operator signal and the audit evidence land together      or neither lands.  The 202 response carries `accepted_at` (server commit timestamp) and `violation_count` (echo of the persisted batch size) so the agent can record the receipt and reconcile with its local replay-queue. The response carries no payload echo and no per-row identifiers; the operator UI consumes the persisted rows from the dedicated audit and integrity surfaces.  DEFERRED-WIRING POSTURE: until the production composition root supplies the IntegrityViolationsRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: integrity_violations_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the reporting Node.
		@return ApiPostNodeIntegrityViolationsRequest
	*/
	PostNodeIntegrityViolations(ctx context.Context, id string) ApiPostNodeIntegrityViolationsRequest

	// PostNodeIntegrityViolationsExecute executes the request
	//  @return IntegrityViolationsResponse
	PostNodeIntegrityViolationsExecute(r ApiPostNodeIntegrityViolationsRequest) (*IntegrityViolationsResponse, *http.Response, error)

	/*
		PostNodeKeysRotate Trigger a mesh-key rotation for a Node.

		Asks the control plane to dispatch a `rotate_keys` command to the addressed Node so it generates a fresh Curve25519 keypair without re-enrolling from scratch. An operator calls this when a Node's mesh key is aged or possibly compromised. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the operator-action ReBAC relation `node-operate`      on `node:<id>` BEFORE any existence check, so an      unauthorised caller cannot probe Node existence via the      404/403 timing differential. A caller lacking the      relation receives 403 `insufficient_relation` and an      audit row with outcome `insufficient_relation`.   3. Records a pending `peer_key_rotation` row and appends      exactly one `rotate_keys` outbox event in a single      transaction. A second trigger against a Node that already      has a pending rotation returns the existing rotation      handle and appends no second event — the operation is      idempotent.  The 200 response carries the rotation handle: the `peer_key_rotation` row id, the live Peer id, and `already_pending` (true when the response reflects a pre-existing in-flight rotation rather than a fresh request).  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationRequester and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the rotation scope.
		@return ApiPostNodeKeysRotateRequest
	*/
	PostNodeKeysRotate(ctx context.Context, id string) ApiPostNodeKeysRotateRequest

	// PostNodeKeysRotateExecute executes the request
	//  @return RotationTriggerResponse
	PostNodeKeysRotateExecute(r ApiPostNodeKeysRotateRequest) (*RotationTriggerResponse, *http.Response, error)

	/*
		PostNodeLogs Ingest a batch of structured log lines for a Node.

		Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `LogLine` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki plus optional SIEM forwarding. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `severity` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: log labels carry Domain, Project, and Node — never an Identity subject or email. Log lines that inadvertently contain PII are the customer's ingest-side responsibility; an optional per-Domain regex redaction filter, disabled by default, can be enabled at ingest.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the ingest scope.
		@return ApiPostNodeLogsRequest
	*/
	PostNodeLogs(ctx context.Context, id string) ApiPostNodeLogsRequest

	// PostNodeLogsExecute executes the request
	//  @return IngestReceipt
	PostNodeLogsExecute(r ApiPostNodeLogsRequest) (*IngestReceipt, *http.Response, error)

	/*
		PostNodeMetrics Ingest a batch of metric samples for a Node.

		Accepts a gzip-compressed JSON array of `MetricSample` records pushed by plexd and hands the batch to the per-signal ingest buffer — a JetStream subject keyed by the originating Node's Domain — from which the platform fans out to Grafana Mimir via Prometheus remote-write. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each record (a malformed record or      a `group` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: metric labels carry Domain, Project, Node, and (where relevant) workload identifier — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the ingest scope.
		@return ApiPostNodeMetricsRequest
	*/
	PostNodeMetrics(ctx context.Context, id string) ApiPostNodeMetricsRequest

	// PostNodeMetricsExecute executes the request
	//  @return IngestReceipt
	PostNodeMetricsExecute(r ApiPostNodeMetricsRequest) (*IngestReceipt, *http.Response, error)

	/*
		PutNodeCapabilities Record the per-Node capability manifest snapshot.

		Accepts a CapabilityManifest snapshot from plexd describing the running agent binary, its SSH host-key fingerprint, and any hooks the agent advertises. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `capabilities_body_too_large` without ever touching      the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_capabilities_request`.   5. Canonicalises the envelope through the      `tenancy.NewCapabilityManifest` value-object constructor      which enforces every manifest invariant: `binary_version`      non-empty, `binary_checksum` exactly 32 bytes,      `ssh_host_key_fingerprint` empty or matching      `SHA256:<base64>`, no duplicate hook names, hook count      within the per-manifest cap, and every hook's `checksum`      exactly 32 bytes.   6. Persists the snapshot inside a single transaction. When      the supplied manifest differs from the prior persisted      row, the recorder appends a `NodeCapabilitiesUpdated`      outbox event in the same transaction so downstream      integrity correlators and dashboard projectors observe      the change atomically.  The 200 response carries `accepted_at` (server commit timestamp), the alphabetically-sorted `fields_changed` list of manifest fields that transitioned versus the prior snapshot, and the dedicated `host_key_changed` flag so downstream security correlators can branch on SSH host-key transitions without parsing `fields_changed`. An idempotent PUT (no diff) emits an empty `fields_changed` array and `host_key_changed: false`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the CapabilitiesRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: capabilities_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the capability manifest scope.
		@return ApiPutNodeCapabilitiesRequest
	*/
	PutNodeCapabilities(ctx context.Context, id string) ApiPutNodeCapabilitiesRequest

	// PutNodeCapabilitiesExecute executes the request
	//  @return CapabilityManifestResponse
	PutNodeCapabilitiesExecute(r ApiPutNodeCapabilitiesRequest) (*CapabilityManifestResponse, *http.Response, error)

	/*
		PutNodeEndpoint Record a NAT-observed Peer endpoint observation.

		Accepts a per-Node NAT endpoint observation from plexd and stamps it onto the Peer aggregate that backs the addressed Node. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 4 KiB; oversize bodies receive      413 `endpoint_body_too_large` without ever touching the      JSON decoder.   4. Validates the body — `reported_at` MUST be within 60s of      server now (otherwise 400 `endpoint_clock_skew`),      `endpoint` MUST parse as a `host:port` tuple in the      RFC 6056 1..65535 port range and a non-loopback IPv4/IPv6      address (otherwise 400 `endpoint_unparseable`), and the      envelope MUST decode without unknown fields (otherwise      400 `malformed_endpoint_request`).   5. Persists the observation through the per-Domain Peer      aggregate writer, which emits a `peer_endpoint_changed`      outbox event when the (endpoint, port) tuple differs from      the prior observation or transitions out of the stale      window; identical refreshes simply refresh the freshness      timestamp without emitting a wake-up.  The 200 response carries `accepted_at` (server commit timestamp) and `stale_after` (`accepted_at` plus the per-Domain endpoint TTL); plexd uses `stale_after` to schedule the next observation before the sweeper would tombstone the endpoint.  DEFERRED-WIRING POSTURE: until the production composition root supplies the EndpointRecorder, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: endpoint_not_provisioned` so log scrapers can alert on the deferred-wiring state.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the endpoint scope.
		@return ApiPutNodeEndpointRequest
	*/
	PutNodeEndpoint(ctx context.Context, id string) ApiPutNodeEndpointRequest

	// PutNodeEndpointExecute executes the request
	//  @return EndpointResponse
	PutNodeEndpointExecute(r ApiPutNodeEndpointRequest) (*EndpointResponse, *http.Response, error)

	/*
		PutNodeStateReport Record an upstream node-state report entry for a Node.

		Records one upstream, workload-authored node-state report entry for the addressed Node under the addressed key. The entry is stored with `kind = report`; report entries are Node-authored telemetry and — unlike the platform-owned metadata/data entries — do NOT fan out a `node_state_updated` event.  The caller authenticates with its Node Secret Key (NSK) in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only write its own reports. The stored entry surfaces in the `reports` bucket of the `state`/`reports` blocks on the next `GET /v1/nodes/{id}/state` reconciliation pull.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`.

		@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
		@param id Node identifier (UUIDv7) — the report-write scope.
		@param key Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).
		@return ApiPutNodeStateReportRequest
	*/
	PutNodeStateReport(ctx context.Context, id string, key string) ApiPutNodeStateReportRequest

	// PutNodeStateReportExecute executes the request
	//  @return NodeStateReportResponse
	PutNodeStateReportExecute(r ApiPutNodeStateReportRequest) (*NodeStateReportResponse, *http.Response, error)
}

// MeshAPIService MeshAPI service
type MeshAPIService service

type ApiDeleteNodeStateReportRequest struct {
	ctx           context.Context
	ApiService    MeshAPI
	id            string
	key           string
	authorization *string
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key. Only a Node proving possession of its NSK may delete a report; a missing, malformed, or revoked credential surfaces as 401.
func (r ApiDeleteNodeStateReportRequest) Authorization(authorization string) ApiDeleteNodeStateReportRequest {
	r.authorization = &authorization
	return r
}

func (r ApiDeleteNodeStateReportRequest) Execute() (*http.Response, error) {
	return r.ApiService.DeleteNodeStateReportExecute(r)
}

/*
DeleteNodeStateReport Delete an upstream node-state report entry for a Node.

Deletes the upstream node-state report entry the addressed Node recorded under the addressed key. Deleting a report emits NO `node_state_updated` event. A delete for a key the Node carries no report under is answered with 404.  The caller authenticates with its NSK in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only delete its own reports.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the report-delete scope.
	@param key Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).
	@return ApiDeleteNodeStateReportRequest
*/
func (a *MeshAPIService) DeleteNodeStateReport(ctx context.Context, id string, key string) ApiDeleteNodeStateReportRequest {
	return ApiDeleteNodeStateReportRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
		key:        key,
	}
}

// Execute executes the request
func (a *MeshAPIService) DeleteNodeStateReportExecute(r ApiDeleteNodeStateReportRequest) (*http.Response, error) {
	var (
		localVarHTTPMethod = http.MethodDelete
		localVarPostBody   interface{}
		formFiles          []formFile
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.DeleteNodeStateReport")
	if err != nil {
		return nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/state/reports/{key}"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)
	localVarPath = strings.Replace(localVarPath, "{"+"key"+"}", url.PathEscape(parameterValueToString(r.key, "key")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return nil, reportError("authorization is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarHTTPResponse, newErr
	}

	return localVarHTTPResponse, nil
}

type ApiFetchNodeSecretRequest struct {
	ctx           context.Context
	ApiService    MeshAPI
	id            string
	name          string
	authorization *string
	version       *int32
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may fetch a secret; a missing, malformed, or revoked credential surfaces as 401.
func (r ApiFetchNodeSecretRequest) Authorization(authorization string) ApiFetchNodeSecretRequest {
	r.authorization = &authorization
	return r
}

// Optional OpenBao KV v2 version to fetch. When omitted the current version is served. The version actually served is always reported in the &#x60;X-Plexsphere-Secret-Version&#x60; response header.
func (r ApiFetchNodeSecretRequest) Version(version int32) ApiFetchNodeSecretRequest {
	r.version = &version
	return r
}

func (r ApiFetchNodeSecretRequest) Execute() (*os.File, *http.Response, error) {
	return r.ApiService.FetchNodeSecretExecute(r)
}

/*
FetchNodeSecret Fetch a Secret Store entry rewrapped under the calling Node's NSK.

Returns the named Secret Store entry for the addressed Node as an AES-256-GCM ciphertext envelope rewrapped under the calling Node's Node Secret Key (NSK). The Secret Store is the platform's only meet-and-rewrap point between the OpenBao backend and the per-Node NSK: the handler reads the OpenBao plaintext, recovers the NSK, and rewraps the payload in-process so that ONLY NSK-wrapped ciphertext ever crosses the wire. The transient OpenBao plaintext and the recovered NSK are zeroed on every exit path; no plaintext is ever persisted, logged, cached, or placed on the event bus.  The 200 body is the raw envelope `<12-byte nonce> || <ciphertext + 16-byte GCM tag>`, served as `application/octet-stream`. The caller recovers the seeded plaintext byte-for-byte with `AES-256-GCM-Open` under its NSK. The response carries the served version in `X-Plexsphere-Secret-Version`, the NSK key id used for the wrap in `X-Plexsphere-Secret-KID` (so plexd can pick the right NSK during a rotation overlap), and `Cache-Control: no-store` so no intermediary ever retains the ciphertext.  The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401. Only a      Node proving possession of its NSK can reach the rewrap      pipeline.   2. Resolves the addressed secret metadata, applies the      per-Node and per-Domain rate limits, and runs the      `node-agent` ReBAC visibility check on the addressed Node.      An authenticated caller without the relation receives 403      `PermissionDenied`; the denial is recorded with      `insufficient_relation`.   3. Reads the entry from the OpenBao backend — the current      version when `version` is omitted, or the requested version      when present — reconciling the served version against the      metadata; the OpenBao-returned version wins for the header.   4. Rewraps the payload under the recovered NSK with a fresh      12-byte nonce and emits exactly one granted audit row. A      backend read that fails AFTER a granted authorization still      records the granted audit row — operational outcomes are      carried on the fetch-duration metric, never on the audit      chain.  The response body is capped at 1 MiB of ciphertext.  DEFERRED-WIRING POSTURE: when the Secret Store backend is not configured (empty backend DSN) the production composition root leaves this surface disabled and every request returns 501 with `code: secrets_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the secret-fetch scope.
	@param name Secret name within the owning Project. Lower-case, starts with a letter, and is limited to letters, digits, hyphen, and underscore (1 to 63 characters).
	@return ApiFetchNodeSecretRequest
*/
func (a *MeshAPIService) FetchNodeSecret(ctx context.Context, id string, name string) ApiFetchNodeSecretRequest {
	return ApiFetchNodeSecretRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
		name:       name,
	}
}

// Execute executes the request
//
//	@return *os.File
func (a *MeshAPIService) FetchNodeSecretExecute(r ApiFetchNodeSecretRequest) (*os.File, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *os.File
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.FetchNodeSecret")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/secrets/{name}"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)
	localVarPath = strings.Replace(localVarPath, "{"+"name"+"}", url.PathEscape(parameterValueToString(r.name, "name")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}

	if r.version != nil {
		parameterAddToHeaderOrQuery(localVarQueryParams, "version", r.version, "form", "")
	}
	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/octet-stream", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 429 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiGetDomainMeshTopologyRequest struct {
	ctx        context.Context
	ApiService MeshAPI
	domainId   string
}

func (r ApiGetDomainMeshTopologyRequest) Execute() (*MeshTopology, *http.Response, error) {
	return r.ApiService.GetDomainMeshTopologyExecute(r)
}

/*
GetDomainMeshTopology Return the mesh topology for a Domain.

Returns the read-side projection of the mesh fabric that backs the dashboard mesh-map view and the `plexctl mesh topology` CLI: every live Peer Node anchored to the Domain together with the directed edges between them. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the `domain-view` ReBAC relation on the addressed      Domain BEFORE any existence check so the endpoint cannot      be used as a Domain-id oracle.   3. Resolves the per-Domain peer graph from the SSE peer-      delta projection — `nodes` carries each anchored Node's      mesh-IP and reachability, `edges` carries each pairwise      relationship with its `mode` (`direct` or `relayed`), the      handshake age, and the bridge Node currently serving as      the relay fallback when one is assigned.  The payload mirrors the SSE peer-graph events one-for-one so a renderer that consumes the live stream and a renderer that polls this pull converge on byte-identical state.  DEFERRED-WIRING POSTURE: until the production composition root supplies the mesh-topology projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: mesh_topology_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param domainId Domain identifier (UUIDv7) — the topology scope.
	@return ApiGetDomainMeshTopologyRequest
*/
func (a *MeshAPIService) GetDomainMeshTopology(ctx context.Context, domainId string) ApiGetDomainMeshTopologyRequest {
	return ApiGetDomainMeshTopologyRequest{
		ApiService: a,
		ctx:        ctx,
		domainId:   domainId,
	}
}

// Execute executes the request
//
//	@return MeshTopology
func (a *MeshAPIService) GetDomainMeshTopologyExecute(r ApiGetDomainMeshTopologyRequest) (*MeshTopology, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *MeshTopology
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.GetDomainMeshTopology")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/domains/{domainId}/mesh/topology"
	localVarPath = strings.Replace(localVarPath, "{"+"domainId"+"}", url.PathEscape(parameterValueToString(r.domainId, "domainId")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiGetNodeEventsRequest struct {
	ctx         context.Context
	ApiService  MeshAPI
	id          string
	lastEventID *string
}

// SSE replay cursor. When present and parseable as a non-negative integer, the server resumes streaming from the envelope whose JetStream sequence is strictly greater than the supplied value. When absent or empty the stream tails from now.
func (r ApiGetNodeEventsRequest) LastEventID(lastEventID string) ApiGetNodeEventsRequest {
	r.lastEventID = &lastEventID
	return r
}

func (r ApiGetNodeEventsRequest) Execute() (string, *http.Response, error) {
	return r.ApiService.GetNodeEventsExecute(r)
}

/*
GetNodeEvents Stream signed envelope events for a Node over SSE.

Opens a Server-Sent Events stream of canonical signed `Envelope` records scoped to the addressed Node. Each `data:` frame is the JSON wire body produced by `internal/signing/envelope.CanonicalBytes`. The server runs ed25519 verification against the per-Domain signing public key before emitting the frame; consumers SHOULD verify the trailing `signature:` field again as a defence-in-depth measure.  The `id:` field on every event frame is the JetStream stream sequence number for that envelope. Clients resume by re- connecting with `Last-Event-ID: <numeric>` set to the last sequence they durably processed; the server replays from the next sequence (`> last`). When the header is absent or empty the stream tails from now — historical events are NOT backfilled.  The server emits a 25-second SSE comment-frame keep-alive (`:keep-alive\\n\\n`) so idle proxies do not collapse the connection. The `X-Plexsphere-API-Version` response header carries the contract version the stream conforms to; `Cache-Control: no-cache` opts the response out of any intermediary caching layer.  DEFERRED-WIRING POSTURE: the production composition root does not yet supply the SignatureVerifier, RelationChecker, or NodeRepo ports the handler depends on. Until those ports are wired, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned`. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the SSE stream scope.
	@return ApiGetNodeEventsRequest
*/
func (a *MeshAPIService) GetNodeEvents(ctx context.Context, id string) ApiGetNodeEventsRequest {
	return ApiGetNodeEventsRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return string
func (a *MeshAPIService) GetNodeEventsExecute(r ApiGetNodeEventsRequest) (string, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue string
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.GetNodeEvents")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/events"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"text/event-stream", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	if r.lastEventID != nil {
		parameterAddToHeaderOrQuery(localVarHeaderParams, "Last-Event-ID", r.lastEventID, "simple", "")
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiGetNodeKeysRotatePreviewRequest struct {
	ctx        context.Context
	ApiService MeshAPI
	id         string
}

func (r ApiGetNodeKeysRotatePreviewRequest) Execute() (*RotationImpactPreview, *http.Response, error) {
	return r.ApiService.GetNodeKeysRotatePreviewExecute(r)
}

/*
GetNodeKeysRotatePreview Preview the fleet impact of rotating a Node's mesh key.

Returns a non-mutating dry-run of the fleet impact a manual mesh-key rotation against the addressed Node would have. Backs the Phase-2 dashboard \"manual key-rotation with impact preview\" view and the `plexctl key rotate --preview` CLI. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the same operator-action ReBAC relation      `node-operate` on `node:<id>` that the mutating      `POST /v1/nodes/{id}/keys/rotate` checks, BEFORE any      existence check, so an unauthorised caller cannot probe      Node existence via the 404/403 timing differential. A      caller lacking the relation receives 403      `insufficient_relation` and an audit row with outcome      `insufficient_relation`.   3. Resolves the per-Node read-side projection — the live      Peer the rotation would target, every peer edge whose      PSK would be re-issued, and the in-flight rotation row      (if any). NO `peer_key_rotation` row is created, NO      `rotate_keys` outbox event is appended, and NO audit row      beyond the read-side ReBAC decision is emitted.  The 200 response payload mirrors the mutating endpoint's view of the world one-for-one — `peer_id` matches what the live `POST` would return, `already_pending` flips on a pre-existing in-flight rotation the same way — so a renderer that consumes both surfaces can drive the dashboard confirmation dialog from a single mapping.  DEFERRED-WIRING POSTURE: until the production composition root supplies the read-side preview projection and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_preview_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the rotation scope.
	@return ApiGetNodeKeysRotatePreviewRequest
*/
func (a *MeshAPIService) GetNodeKeysRotatePreview(ctx context.Context, id string) ApiGetNodeKeysRotatePreviewRequest {
	return ApiGetNodeKeysRotatePreviewRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return RotationImpactPreview
func (a *MeshAPIService) GetNodeKeysRotatePreviewExecute(r ApiGetNodeKeysRotatePreviewRequest) (*RotationImpactPreview, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *RotationImpactPreview
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.GetNodeKeysRotatePreview")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/keys/rotate/preview"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiGetNodeReachabilityRequest struct {
	ctx        context.Context
	ApiService MeshAPI
	id         string
}

func (r ApiGetNodeReachabilityRequest) Execute() (*Reachability, *http.Response, error) {
	return r.ApiService.GetNodeReachabilityExecute(r)
}

/*
GetNodeReachability Read the reachability projection for a Node.

Returns the latest `Reachability` projection for the addressed Node — the per-Node state-machine the heartbeat handler advances on every accepted heartbeat. The projection is the authoritative health view the operator UI and reconciliation surface consume; it transitions `healthy` → `stale` after 90s without a heartbeat and `stale` → `unreachable` after 300s, with `changed_at` tracking the most recent transition.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/state` and `GET /v1/nodes/{id}/events`, so any caller authorised to issue a reconciliation pull is also authorised to read the reachability projection. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle .  DEFERRED-WIRING POSTURE: until the production composition root supplies the ReachabilityRepo, RelationChecker, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: reachability_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the reachability scope.
	@return ApiGetNodeReachabilityRequest
*/
func (a *MeshAPIService) GetNodeReachability(ctx context.Context, id string) ApiGetNodeReachabilityRequest {
	return ApiGetNodeReachabilityRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return Reachability
func (a *MeshAPIService) GetNodeReachabilityExecute(r ApiGetNodeReachabilityRequest) (*Reachability, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *Reachability
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.GetNodeReachability")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/reachability"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiGetNodeStateRequest struct {
	ctx        context.Context
	ApiService MeshAPI
	id         string
}

func (r ApiGetNodeStateRequest) Execute() (*NodeStateSnapshot, *http.Response, error) {
	return r.ApiService.GetNodeStateExecute(r)
}

/*
GetNodeState Reconciliation pull for a Node — return the canonical NodeStateSnapshot.

Returns the canonical `NodeStateSnapshot` for the addressed Node. The snapshot is the authoritative cold-start view that plexd consumes when it first comes up, when its SSE connection has been disconnected for longer than the replay window, or when an out-of-band request arrives to re-derive the desired state. The four wire blocks — `peers`, `policy`, `bridge`, and `state`/`reports` — are always present so plexd's reconcile loop can diff by field presence rather than absence; later stories populate the currently-empty blocks without changing the wire shape .  The peer projection is a single SQL round-trip ordered by `node_id ASC` so two consecutive pulls against the same ledger snapshot are byte-equal — plexd's reconcile loop reduces redundant rewrites by hashing the response.  The endpoint reuses the `node-agent` ReBAC relation already guarding `GET /v1/nodes/{id}/events`, so any caller authorised to subscribe to a Node's SSE event stream is also authorised to issue a reconciliation pull. Unauthenticated callers receive 401; authenticated callers without the relation receive 403; an unknown Node id surfaces as 404 only after the authorisation gate passes so the endpoint cannot be used as a Node-id oracle.  DEFERRED-WIRING POSTURE: until the production composition root supplies the `SnapshotProvider`, `RelationChecker`, and `NodeRepo` ports the handler depends on, every request to this endpoint returns 501 with `code: signed_event_bus_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the snapshot scope.
	@return ApiGetNodeStateRequest
*/
func (a *MeshAPIService) GetNodeState(ctx context.Context, id string) ApiGetNodeStateRequest {
	return ApiGetNodeStateRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return NodeStateSnapshot
func (a *MeshAPIService) GetNodeStateExecute(r ApiGetNodeStateRequest) (*NodeStateSnapshot, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodGet
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *NodeStateSnapshot
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.GetNodeState")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/state"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostKeysRotateRequest struct {
	ctx               context.Context
	ApiService        MeshAPI
	keysRotateRequest *KeysRotateRequest
}

func (r ApiPostKeysRotateRequest) KeysRotateRequest(keysRotateRequest KeysRotateRequest) ApiPostKeysRotateRequest {
	r.keysRotateRequest = &keysRotateRequest
	return r
}

func (r ApiPostKeysRotateRequest) Execute() (*KeysRotateResponse, *http.Response, error) {
	return r.ApiService.PostKeysRotateExecute(r)
}

/*
PostKeysRotate Submit a freshly-generated Curve25519 key to complete a rotation.

Completes a pending mesh-key rotation for the calling Node. After an operator triggers a rotation via `POST /v1/nodes/{id}/keys/rotate` — or the heartbeat response sets `rotate_keys: true` — plexd generates a fresh Curve25519 keypair and submits the new public key here. The handler:    1. Authenticates the caller against the Node Secret Key      (NSK) plaintext supplied in the `Authorization: Bearer`      header. The route carries NO path `{id}` segment — the      rotating Node is identified solely by the NSK envelope it      presents — so a missing or unresolved NSK surfaces as      401.   2. Caps the request body at 4 KiB; oversize bodies receive      413 `keys_rotate_body_too_large` without ever touching      the JSON decoder.   3. Validates the submitted `new_public_key` — it MUST decode      to exactly 32 bytes and MUST NOT be the all-zero      degenerate value — BEFORE any database read (otherwise      422 `keys_rotate_public_key_invalid`).   4. Resolves the caller's live Peer row; a Node with no live      Peer surfaces as 404 `keys_rotate_peer_not_found`.   5. Hands the new key to the rotation completer, which — in      one transaction — updates `nodes.public_key`, retires the      old pairwise PSK, wraps and inserts a fresh PSK, appends      a `peer_key_rotated` outbox event, and flips the pending      `peer_key_rotation` row to `completed`.  The 200 response carries the completed rotation's id plus the `(kid, wrap_key_version)` reference of the re-issued PSK — it never carries PSK plaintext or ciphertext. An idempotent retry with the same key against an already-completed rotation returns the prior receipt and emits no second event.  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationCompleter, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@return ApiPostKeysRotateRequest
*/
func (a *MeshAPIService) PostKeysRotate(ctx context.Context) ApiPostKeysRotateRequest {
	return ApiPostKeysRotateRequest{
		ApiService: a,
		ctx:        ctx,
	}
}

// Execute executes the request
//
//	@return KeysRotateResponse
func (a *MeshAPIService) PostKeysRotateExecute(r ApiPostKeysRotateRequest) (*KeysRotateResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *KeysRotateResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostKeysRotate")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/keys/rotate"

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.keysRotateRequest == nil {
		return localVarReturnValue, nil, reportError("keysRotateRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	// body params
	localVarPostBody = r.keysRotateRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 409 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 422 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeAuditRequest struct {
	ctx               context.Context
	ApiService        MeshAPI
	id                string
	authorization     *string
	auditEvent        *[]AuditEvent
	xPlexsphereSentAt *time.Time
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPostNodeAuditRequest) Authorization(authorization string) ApiPostNodeAuditRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPostNodeAuditRequest) AuditEvent(auditEvent []AuditEvent) ApiPostNodeAuditRequest {
	r.auditEvent = &auditEvent
	return r
}

// RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 &#x60;ingest_sent_at_invalid&#x60;; it drives the ingest-lag metric the platform records on acceptance.
func (r ApiPostNodeAuditRequest) XPlexsphereSentAt(xPlexsphereSentAt time.Time) ApiPostNodeAuditRequest {
	r.xPlexsphereSentAt = &xPlexsphereSentAt
	return r
}

func (r ApiPostNodeAuditRequest) Execute() (*IngestReceipt, *http.Response, error) {
	return r.ApiService.PostNodeAuditExecute(r)
}

/*
PostNodeAudit Ingest a batch of normalised audit events for a Node.

Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `AuditEvent` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki with a separate retention class and a distinct SIEM route. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `source` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: audit-ingest labels carry Domain, Project, and Node — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the ingest scope.
	@return ApiPostNodeAuditRequest
*/
func (a *MeshAPIService) PostNodeAudit(ctx context.Context, id string) ApiPostNodeAuditRequest {
	return ApiPostNodeAuditRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return IngestReceipt
func (a *MeshAPIService) PostNodeAuditExecute(r ApiPostNodeAuditRequest) (*IngestReceipt, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *IngestReceipt
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeAudit")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/audit"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.auditEvent == nil {
		return localVarReturnValue, nil, reportError("auditEvent is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/x-ndjson"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	if r.xPlexsphereSentAt != nil {
		parameterAddToHeaderOrQuery(localVarHeaderParams, "X-Plexsphere-Sent-At", r.xPlexsphereSentAt, "simple", "")
	}
	// body params
	localVarPostBody = r.auditEvent
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 415 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 429 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeHeartbeatRequest struct {
	ctx              context.Context
	ApiService       MeshAPI
	id               string
	authorization    *string
	heartbeatRequest *HeartbeatRequest
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60; .
func (r ApiPostNodeHeartbeatRequest) Authorization(authorization string) ApiPostNodeHeartbeatRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPostNodeHeartbeatRequest) HeartbeatRequest(heartbeatRequest HeartbeatRequest) ApiPostNodeHeartbeatRequest {
	r.heartbeatRequest = &heartbeatRequest
	return r
}

func (r ApiPostNodeHeartbeatRequest) Execute() (*HeartbeatResponse, *http.Response, error) {
	return r.ApiService.PostNodeHeartbeatExecute(r)
}

/*
PostNodeHeartbeat Record a Node liveness heartbeat and return reconcile/rotate hints.

Accepts a per-Node liveness heartbeat from plexd and updates the reachability projection that drives the mesh-wide health view . The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      rejecting revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Validates the request body — `client_now` MUST be within      60s of server now (otherwise 400 `clock_skew`),      `binary_checksum` MUST be present and decode to a 32-byte      SHA-256 digest (otherwise 400 `binary_checksum_empty`)      .   4. Persists the heartbeat fact and updates the per-Node      reachability state-machine (`healthy` → `stale` after 90s,      `stale` → `unreachable` after 300s) so the projection at      `GET /v1/nodes/{id}/reachability` reflects the new fact      on the next read.  The 200 response carries `accepted_at` (server timestamp at commit) and two reconciliation flags. `reconcile` defaults to `false` and later stories flip it when the controller wants plexd to issue a fresh reconciliation pull. `rotate_keys` is load-bearing: it is set to `true` whenever a `peer_key_rotation` row is pending for the heartbeating Node, telling the caller to generate a fresh Curve25519 keypair and complete the rotation via `POST /v1/keys/rotate`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the NSK validator, ReachabilityRepo, and clock-skew evaluator the handler depends on, every request to this endpoint returns 501 with `code: heartbeat_not_provisioned` so log scrapers can alert on the deferred-wiring state. See docs/architecture/mesh-event-bus-roadmap.md for the deferred work tracking.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the heartbeat scope.
	@return ApiPostNodeHeartbeatRequest
*/
func (a *MeshAPIService) PostNodeHeartbeat(ctx context.Context, id string) ApiPostNodeHeartbeatRequest {
	return ApiPostNodeHeartbeatRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return HeartbeatResponse
func (a *MeshAPIService) PostNodeHeartbeatExecute(r ApiPostNodeHeartbeatRequest) (*HeartbeatResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *HeartbeatResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeHeartbeat")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/heartbeat"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.heartbeatRequest == nil {
		return localVarReturnValue, nil, reportError("heartbeatRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	// body params
	localVarPostBody = r.heartbeatRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeIntegrityViolationsRequest struct {
	ctx                        context.Context
	ApiService                 MeshAPI
	id                         string
	authorization              *string
	integrityViolationsRequest *IntegrityViolationsRequest
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPostNodeIntegrityViolationsRequest) Authorization(authorization string) ApiPostNodeIntegrityViolationsRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPostNodeIntegrityViolationsRequest) IntegrityViolationsRequest(integrityViolationsRequest IntegrityViolationsRequest) ApiPostNodeIntegrityViolationsRequest {
	r.integrityViolationsRequest = &integrityViolationsRequest
	return r
}

func (r ApiPostNodeIntegrityViolationsRequest) Execute() (*IntegrityViolationsResponse, *http.Response, error) {
	return r.ApiService.PostNodeIntegrityViolationsExecute(r)
}

/*
PostNodeIntegrityViolations Ingest a batch of integrity-violation reports for a Node.

Accepts a batch of `IntegrityViolation` reports from plexd describing tamper-evidence divergences the agent detected on the local Node: a mismatched binary checksum, a tampered Lua hook checksum, or a rotated SSH host-key fingerprint. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `integrity_violations_body_too_large` without ever      touching the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_integrity_violations_request`.   5. Canonicalises every entry through the      `tenancy.NewIntegrityViolation` value-object constructor      which enforces every per-violation invariant: `kind` in      the closed set `{binary_checksum, hook_checksum,      ssh_host_key}`, `detected_by` in the closed set      `{startup_scan, inotify, pre_dispatch}`, `artifact_id`      non-empty after trimming, `observed_checksum`/      `expected_checksum` exactly 32 bytes for the checksum      kinds, `observed_fingerprint`/`expected_fingerprint`      matching `SHA256:<base64>` for the `ssh_host_key` kind,      and the per-kind column-mismatch guard that rejects a      checksum kind carrying fingerprint fields and vice versa.   6. Hands the canonical batch to the Node aggregate's      `RecordIntegrityViolations` method which enforces the      batch-level invariants: at least one entry      (`integrity_violations_empty`) and at most 128 entries      (`integrity_violations_too_many`).   7. Persists every violation row and appends a single      `integrity_alert` outbox event inside one transaction so      the operator signal and the audit evidence land together      or neither lands.  The 202 response carries `accepted_at` (server commit timestamp) and `violation_count` (echo of the persisted batch size) so the agent can record the receipt and reconcile with its local replay-queue. The response carries no payload echo and no per-row identifiers; the operator UI consumes the persisted rows from the dedicated audit and integrity surfaces.  DEFERRED-WIRING POSTURE: until the production composition root supplies the IntegrityViolationsRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: integrity_violations_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the reporting Node.
	@return ApiPostNodeIntegrityViolationsRequest
*/
func (a *MeshAPIService) PostNodeIntegrityViolations(ctx context.Context, id string) ApiPostNodeIntegrityViolationsRequest {
	return ApiPostNodeIntegrityViolationsRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return IntegrityViolationsResponse
func (a *MeshAPIService) PostNodeIntegrityViolationsExecute(r ApiPostNodeIntegrityViolationsRequest) (*IntegrityViolationsResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *IntegrityViolationsResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeIntegrityViolations")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/integrity-violations"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.integrityViolationsRequest == nil {
		return localVarReturnValue, nil, reportError("integrityViolationsRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	// body params
	localVarPostBody = r.integrityViolationsRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeKeysRotateRequest struct {
	ctx        context.Context
	ApiService MeshAPI
	id         string
}

func (r ApiPostNodeKeysRotateRequest) Execute() (*RotationTriggerResponse, *http.Response, error) {
	return r.ApiService.PostNodeKeysRotateExecute(r)
}

/*
PostNodeKeysRotate Trigger a mesh-key rotation for a Node.

Asks the control plane to dispatch a `rotate_keys` command to the addressed Node so it generates a fresh Curve25519 keypair without re-enrolling from scratch. An operator calls this when a Node's mesh key is aged or possibly compromised. The handler:    1. Authenticates the caller and rejects requests without a      resolved Principal with 401.   2. Checks the operator-action ReBAC relation `node-operate`      on `node:<id>` BEFORE any existence check, so an      unauthorised caller cannot probe Node existence via the      404/403 timing differential. A caller lacking the      relation receives 403 `insufficient_relation` and an      audit row with outcome `insufficient_relation`.   3. Records a pending `peer_key_rotation` row and appends      exactly one `rotate_keys` outbox event in a single      transaction. A second trigger against a Node that already      has a pending rotation returns the existing rotation      handle and appends no second event — the operation is      idempotent.  The 200 response carries the rotation handle: the `peer_key_rotation` row id, the live Peer id, and `already_pending` (true when the response reflects a pre-existing in-flight rotation rather than a fresh request).  DEFERRED-WIRING POSTURE: until the production composition root supplies the RotationRequester and the ReBAC RelationChecker the handler depends on, every request to this endpoint returns 501 with `code: node_keys_rotate_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the rotation scope.
	@return ApiPostNodeKeysRotateRequest
*/
func (a *MeshAPIService) PostNodeKeysRotate(ctx context.Context, id string) ApiPostNodeKeysRotateRequest {
	return ApiPostNodeKeysRotateRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return RotationTriggerResponse
func (a *MeshAPIService) PostNodeKeysRotateExecute(r ApiPostNodeKeysRotateRequest) (*RotationTriggerResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *RotationTriggerResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeKeysRotate")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/keys/rotate"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeLogsRequest struct {
	ctx               context.Context
	ApiService        MeshAPI
	id                string
	authorization     *string
	logLine           *[]LogLine
	xPlexsphereSentAt *time.Time
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPostNodeLogsRequest) Authorization(authorization string) ApiPostNodeLogsRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPostNodeLogsRequest) LogLine(logLine []LogLine) ApiPostNodeLogsRequest {
	r.logLine = &logLine
	return r
}

// RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 &#x60;ingest_sent_at_invalid&#x60;; it drives the ingest-lag metric the platform records on acceptance.
func (r ApiPostNodeLogsRequest) XPlexsphereSentAt(xPlexsphereSentAt time.Time) ApiPostNodeLogsRequest {
	r.xPlexsphereSentAt = &xPlexsphereSentAt
	return r
}

func (r ApiPostNodeLogsRequest) Execute() (*IngestReceipt, *http.Response, error) {
	return r.ApiService.PostNodeLogsExecute(r)
}

/*
PostNodeLogs Ingest a batch of structured log lines for a Node.

Accepts a gzip-compressed newline-delimited JSON (`application/x-ndjson`) batch of `LogLine` records — one object per line — pushed by plexd and hands the batch to the per-signal ingest buffer (a JetStream subject keyed by the originating Node's Domain), from which the platform routes to Grafana Loki plus optional SIEM forwarding. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each line (a malformed line or a      `severity` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: log labels carry Domain, Project, and Node — never an Identity subject or email. Log lines that inadvertently contain PII are the customer's ingest-side responsibility; an optional per-Domain regex redaction filter, disabled by default, can be enabled at ingest.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the ingest scope.
	@return ApiPostNodeLogsRequest
*/
func (a *MeshAPIService) PostNodeLogs(ctx context.Context, id string) ApiPostNodeLogsRequest {
	return ApiPostNodeLogsRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return IngestReceipt
func (a *MeshAPIService) PostNodeLogsExecute(r ApiPostNodeLogsRequest) (*IngestReceipt, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *IngestReceipt
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeLogs")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/logs"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.logLine == nil {
		return localVarReturnValue, nil, reportError("logLine is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/x-ndjson"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	if r.xPlexsphereSentAt != nil {
		parameterAddToHeaderOrQuery(localVarHeaderParams, "X-Plexsphere-Sent-At", r.xPlexsphereSentAt, "simple", "")
	}
	// body params
	localVarPostBody = r.logLine
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 415 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 429 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPostNodeMetricsRequest struct {
	ctx               context.Context
	ApiService        MeshAPI
	id                string
	authorization     *string
	metricSample      *[]MetricSample
	xPlexsphereSentAt *time.Time
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPostNodeMetricsRequest) Authorization(authorization string) ApiPostNodeMetricsRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPostNodeMetricsRequest) MetricSample(metricSample []MetricSample) ApiPostNodeMetricsRequest {
	r.metricSample = &metricSample
	return r
}

// RFC 3339 timestamp at which plexd dispatched the batch. The handler requires it and refuses a missing or unparseable value with 400 &#x60;ingest_sent_at_invalid&#x60;; it drives the ingest-lag metric the platform records on acceptance.
func (r ApiPostNodeMetricsRequest) XPlexsphereSentAt(xPlexsphereSentAt time.Time) ApiPostNodeMetricsRequest {
	r.xPlexsphereSentAt = &xPlexsphereSentAt
	return r
}

func (r ApiPostNodeMetricsRequest) Execute() (*IngestReceipt, *http.Response, error) {
	return r.ApiService.PostNodeMetricsExecute(r)
}

/*
PostNodeMetrics Ingest a batch of metric samples for a Node.

Accepts a gzip-compressed JSON array of `MetricSample` records pushed by plexd and hands the batch to the per-signal ingest buffer — a JetStream subject keyed by the originating Node's Domain — from which the platform fans out to Grafana Mimir via Prometheus remote-write. Every record is tagged server-side with the Domain and Project of the originating Node so dashboards and alerts scope through the platform label model; the wire body never carries an Identity subject or email (see the de-personalisation note below).  The handler applies the following gates in order; each maps to a single stable `code` on an RFC 9457 Problem body so generated clients can switch on `code` exhaustively:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext in the `Authorization: Bearer` header, refusing a      missing, malformed, or revoked credential with 401.   2. Asserts the NSK belongs to the Node addressed by the path      `id`, refusing cross-Node use with 403 `node_id_mismatch` so      a leaked NSK cannot be replayed against a sibling Node.   3. Refuses a `Content-Encoding` other than `gzip` or `identity`      with 415 `ingest_encoding_unsupported`.   4. Requires the `X-Plexsphere-Sent-At` header to be present and      an RFC 3339 timestamp; a missing or unparseable value is      refused with 400 `ingest_sent_at_invalid`. The header drives      the ingest-lag metric.   5. Caps the compressed wire body; a body over the cap is refused      with 413 `ingest_body_too_large` before the gzip reader runs.   6. Applies the per-Node then per-Domain byte-weighted quota; an      exhausted per-Node bucket surfaces as 429      `per_node_rate_limited` and an exhausted per-Domain bucket as      429 `capacity_exceeded`, both carrying a `Retry-After` header.   7. Inflates the gzip stream under a decompression cap (a stream      that fails to inflate is 400 `ingest_encoding_invalid`; an      inflated batch over the cap is 413 `ingest_body_too_large`)      and structurally validates each record (a malformed record or      a `group` outside the closed set is 400      `ingest_batch_malformed`; more than the per-batch record cap      is 413 `ingest_batch_too_many_records`).   8. Publishes the batch to the ingest buffer; a buffer the stream      backend cannot accept is 503 `ingest_buffer_unavailable` with      a `Retry-After` header.  On success the handler records the ingest-lag, byte, and record metrics and returns 202 with an `IngestReceipt` carrying the server acceptance timestamp and the accepted record count.  DE-PERSONALISATION: metric labels carry Domain, Project, Node, and (where relevant) workload identifier — never an Identity subject or email. PII that a record inadvertently carries is the customer's ingest-side responsibility.  DEFERRED-WIRING POSTURE: when no observability ingest backend is configured (empty `PLEXSPHERE_OBS_NATS_URL`) the production composition root leaves this surface disabled and every request returns 501 with `code: observability_ingest_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the ingest scope.
	@return ApiPostNodeMetricsRequest
*/
func (a *MeshAPIService) PostNodeMetrics(ctx context.Context, id string) ApiPostNodeMetricsRequest {
	return ApiPostNodeMetricsRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return IngestReceipt
func (a *MeshAPIService) PostNodeMetricsExecute(r ApiPostNodeMetricsRequest) (*IngestReceipt, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPost
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *IngestReceipt
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PostNodeMetrics")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/metrics"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.metricSample == nil {
		return localVarReturnValue, nil, reportError("metricSample is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	if r.xPlexsphereSentAt != nil {
		parameterAddToHeaderOrQuery(localVarHeaderParams, "X-Plexsphere-Sent-At", r.xPlexsphereSentAt, "simple", "")
	}
	// body params
	localVarPostBody = r.metricSample
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 415 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 429 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 503 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPutNodeCapabilitiesRequest struct {
	ctx                       context.Context
	ApiService                MeshAPI
	id                        string
	authorization             *string
	capabilityManifestRequest *CapabilityManifestRequest
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPutNodeCapabilitiesRequest) Authorization(authorization string) ApiPutNodeCapabilitiesRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPutNodeCapabilitiesRequest) CapabilityManifestRequest(capabilityManifestRequest CapabilityManifestRequest) ApiPutNodeCapabilitiesRequest {
	r.capabilityManifestRequest = &capabilityManifestRequest
	return r
}

func (r ApiPutNodeCapabilitiesRequest) Execute() (*CapabilityManifestResponse, *http.Response, error) {
	return r.ApiService.PutNodeCapabilitiesExecute(r)
}

/*
PutNodeCapabilities Record the per-Node capability manifest snapshot.

Accepts a CapabilityManifest snapshot from plexd describing the running agent binary, its SSH host-key fingerprint, and any hooks the agent advertises. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 32 KiB; oversize bodies receive      413 `capabilities_body_too_large` without ever touching      the JSON decoder.   4. Decodes the body with `DisallowUnknownFields`; a malformed      envelope or an unknown field surfaces as 400      `malformed_capabilities_request`.   5. Canonicalises the envelope through the      `tenancy.NewCapabilityManifest` value-object constructor      which enforces every manifest invariant: `binary_version`      non-empty, `binary_checksum` exactly 32 bytes,      `ssh_host_key_fingerprint` empty or matching      `SHA256:<base64>`, no duplicate hook names, hook count      within the per-manifest cap, and every hook's `checksum`      exactly 32 bytes.   6. Persists the snapshot inside a single transaction. When      the supplied manifest differs from the prior persisted      row, the recorder appends a `NodeCapabilitiesUpdated`      outbox event in the same transaction so downstream      integrity correlators and dashboard projectors observe      the change atomically.  The 200 response carries `accepted_at` (server commit timestamp), the alphabetically-sorted `fields_changed` list of manifest fields that transitioned versus the prior snapshot, and the dedicated `host_key_changed` flag so downstream security correlators can branch on SSH host-key transitions without parsing `fields_changed`. An idempotent PUT (no diff) emits an empty `fields_changed` array and `host_key_changed: false`.  DEFERRED-WIRING POSTURE: until the production composition root supplies the CapabilitiesRecorder, NSKResolver, and NodeRepo ports the handler depends on, every request to this endpoint returns 501 with `code: capabilities_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the capability manifest scope.
	@return ApiPutNodeCapabilitiesRequest
*/
func (a *MeshAPIService) PutNodeCapabilities(ctx context.Context, id string) ApiPutNodeCapabilitiesRequest {
	return ApiPutNodeCapabilitiesRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return CapabilityManifestResponse
func (a *MeshAPIService) PutNodeCapabilitiesExecute(r ApiPutNodeCapabilitiesRequest) (*CapabilityManifestResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPut
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *CapabilityManifestResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PutNodeCapabilities")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/capabilities"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.capabilityManifestRequest == nil {
		return localVarReturnValue, nil, reportError("capabilityManifestRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	// body params
	localVarPostBody = r.capabilityManifestRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 422 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPutNodeEndpointRequest struct {
	ctx             context.Context
	ApiService      MeshAPI
	id              string
	authorization   *string
	endpointRequest *EndpointRequest
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. The NSK is bound to the Node addressed by the path &#x60;id&#x60;; a credential belonging to a different Node surfaces as 403 &#x60;node_id_mismatch&#x60;.
func (r ApiPutNodeEndpointRequest) Authorization(authorization string) ApiPutNodeEndpointRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPutNodeEndpointRequest) EndpointRequest(endpointRequest EndpointRequest) ApiPutNodeEndpointRequest {
	r.endpointRequest = &endpointRequest
	return r
}

func (r ApiPutNodeEndpointRequest) Execute() (*EndpointResponse, *http.Response, error) {
	return r.ApiService.PutNodeEndpointExecute(r)
}

/*
PutNodeEndpoint Record a NAT-observed Peer endpoint observation.

Accepts a per-Node NAT endpoint observation from plexd and stamps it onto the Peer aggregate that backs the addressed Node. The handler:    1. Authenticates the caller against the Node Secret Key (NSK)      plaintext supplied in the `Authorization: Bearer` header,      refusing missing or revoked credentials with 401.   2. Asserts that the NSK belongs to the Node addressed by the      path `id`, refusing cross-Node use with 403      `node_id_mismatch` so a leaked NSK cannot be replayed      against a sibling Node.   3. Caps the request body at 4 KiB; oversize bodies receive      413 `endpoint_body_too_large` without ever touching the      JSON decoder.   4. Validates the body — `reported_at` MUST be within 60s of      server now (otherwise 400 `endpoint_clock_skew`),      `endpoint` MUST parse as a `host:port` tuple in the      RFC 6056 1..65535 port range and a non-loopback IPv4/IPv6      address (otherwise 400 `endpoint_unparseable`), and the      envelope MUST decode without unknown fields (otherwise      400 `malformed_endpoint_request`).   5. Persists the observation through the per-Domain Peer      aggregate writer, which emits a `peer_endpoint_changed`      outbox event when the (endpoint, port) tuple differs from      the prior observation or transitions out of the stale      window; identical refreshes simply refresh the freshness      timestamp without emitting a wake-up.  The 200 response carries `accepted_at` (server commit timestamp) and `stale_after` (`accepted_at` plus the per-Domain endpoint TTL); plexd uses `stale_after` to schedule the next observation before the sweeper would tombstone the endpoint.  DEFERRED-WIRING POSTURE: until the production composition root supplies the EndpointRecorder, NSKResolver, NodeRepo, and PeerLookup ports the handler depends on, every request to this endpoint returns 501 with `code: endpoint_not_provisioned` so log scrapers can alert on the deferred-wiring state.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the endpoint scope.
	@return ApiPutNodeEndpointRequest
*/
func (a *MeshAPIService) PutNodeEndpoint(ctx context.Context, id string) ApiPutNodeEndpointRequest {
	return ApiPutNodeEndpointRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
	}
}

// Execute executes the request
//
//	@return EndpointResponse
func (a *MeshAPIService) PutNodeEndpointExecute(r ApiPutNodeEndpointRequest) (*EndpointResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPut
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *EndpointResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PutNodeEndpoint")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/endpoint"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.endpointRequest == nil {
		return localVarReturnValue, nil, reportError("endpointRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	// body params
	localVarPostBody = r.endpointRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 410 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}

type ApiPutNodeStateReportRequest struct {
	ctx                    context.Context
	ApiService             MeshAPI
	id                     string
	key                    string
	authorization          *string
	nodeStateReportRequest *NodeStateReportRequest
}

// &#x60;Bearer &lt;NSK plaintext&gt;&#x60; — the per-Node Node Secret Key issued at registration time. Only a Node proving possession of its NSK may write a report; a missing, malformed, or revoked credential surfaces as 401.
func (r ApiPutNodeStateReportRequest) Authorization(authorization string) ApiPutNodeStateReportRequest {
	r.authorization = &authorization
	return r
}

func (r ApiPutNodeStateReportRequest) NodeStateReportRequest(nodeStateReportRequest NodeStateReportRequest) ApiPutNodeStateReportRequest {
	r.nodeStateReportRequest = &nodeStateReportRequest
	return r
}

func (r ApiPutNodeStateReportRequest) Execute() (*NodeStateReportResponse, *http.Response, error) {
	return r.ApiService.PutNodeStateReportExecute(r)
}

/*
PutNodeStateReport Record an upstream node-state report entry for a Node.

Records one upstream, workload-authored node-state report entry for the addressed Node under the addressed key. The entry is stored with `kind = report`; report entries are Node-authored telemetry and — unlike the platform-owned metadata/data entries — do NOT fan out a `node_state_updated` event.  The caller authenticates with its Node Secret Key (NSK) in the `Authorization: Bearer` header; the handler asserts the resolved NSK Node matches the path `id` so a Node may only write its own reports. The stored entry surfaces in the `reports` bucket of the `state`/`reports` blocks on the next `GET /v1/nodes/{id}/state` reconciliation pull.  DEFERRED-WIRING POSTURE: when the node-state service is not wired into this build every request returns 501 with `code: reports_not_provisioned`.

	@param ctx context.Context - for authentication, logging, cancellation, deadlines, tracing, etc. Passed from http.Request or context.Background().
	@param id Node identifier (UUIDv7) — the report-write scope.
	@param key Report entry key. Lower-case, starts with a letter, and is limited to letters, digits, dot, hyphen, and underscore (1 to 128 characters).
	@return ApiPutNodeStateReportRequest
*/
func (a *MeshAPIService) PutNodeStateReport(ctx context.Context, id string, key string) ApiPutNodeStateReportRequest {
	return ApiPutNodeStateReportRequest{
		ApiService: a,
		ctx:        ctx,
		id:         id,
		key:        key,
	}
}

// Execute executes the request
//
//	@return NodeStateReportResponse
func (a *MeshAPIService) PutNodeStateReportExecute(r ApiPutNodeStateReportRequest) (*NodeStateReportResponse, *http.Response, error) {
	var (
		localVarHTTPMethod  = http.MethodPut
		localVarPostBody    interface{}
		formFiles           []formFile
		localVarReturnValue *NodeStateReportResponse
	)

	localBasePath, err := a.client.cfg.ServerURLWithContext(r.ctx, "MeshAPIService.PutNodeStateReport")
	if err != nil {
		return localVarReturnValue, nil, &GenericOpenAPIError{error: err.Error()}
	}

	localVarPath := localBasePath + "/v1/nodes/{id}/state/reports/{key}"
	localVarPath = strings.Replace(localVarPath, "{"+"id"+"}", url.PathEscape(parameterValueToString(r.id, "id")), -1)
	localVarPath = strings.Replace(localVarPath, "{"+"key"+"}", url.PathEscape(parameterValueToString(r.key, "key")), -1)

	localVarHeaderParams := make(map[string]string)
	localVarQueryParams := url.Values{}
	localVarFormParams := url.Values{}
	if r.authorization == nil {
		return localVarReturnValue, nil, reportError("authorization is required and must be specified")
	}
	if r.nodeStateReportRequest == nil {
		return localVarReturnValue, nil, reportError("nodeStateReportRequest is required and must be specified")
	}

	// to determine the Content-Type header
	localVarHTTPContentTypes := []string{"application/json"}

	// set Content-Type header
	localVarHTTPContentType := selectHeaderContentType(localVarHTTPContentTypes)
	if localVarHTTPContentType != "" {
		localVarHeaderParams["Content-Type"] = localVarHTTPContentType
	}

	// to determine the Accept header
	localVarHTTPHeaderAccepts := []string{"application/json", "application/problem+json"}

	// set Accept header
	localVarHTTPHeaderAccept := selectHeaderAccept(localVarHTTPHeaderAccepts)
	if localVarHTTPHeaderAccept != "" {
		localVarHeaderParams["Accept"] = localVarHTTPHeaderAccept
	}
	parameterAddToHeaderOrQuery(localVarHeaderParams, "Authorization", r.authorization, "simple", "")
	// body params
	localVarPostBody = r.nodeStateReportRequest
	req, err := a.client.prepareRequest(r.ctx, localVarPath, localVarHTTPMethod, localVarPostBody, localVarHeaderParams, localVarQueryParams, localVarFormParams, formFiles)
	if err != nil {
		return localVarReturnValue, nil, err
	}

	localVarHTTPResponse, err := a.client.callAPI(req)
	if err != nil || localVarHTTPResponse == nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	localVarBody, err := io.ReadAll(localVarHTTPResponse.Body)
	localVarHTTPResponse.Body.Close()
	localVarHTTPResponse.Body = io.NopCloser(bytes.NewBuffer(localVarBody))
	if err != nil {
		return localVarReturnValue, localVarHTTPResponse, err
	}

	if localVarHTTPResponse.StatusCode >= 300 {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: localVarHTTPResponse.Status,
		}
		if localVarHTTPResponse.StatusCode == 400 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 401 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 403 {
			var v PermissionDenied
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 404 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 413 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 501 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
			return localVarReturnValue, localVarHTTPResponse, newErr
		}
		if localVarHTTPResponse.StatusCode == 500 {
			var v Problem
			err = a.client.decode(&v, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
			if err != nil {
				newErr.error = err.Error()
				return localVarReturnValue, localVarHTTPResponse, newErr
			}
			newErr.error = formatErrorMessage(localVarHTTPResponse.Status, &v)
			newErr.model = v
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	err = a.client.decode(&localVarReturnValue, localVarBody, localVarHTTPResponse.Header.Get("Content-Type"))
	if err != nil {
		newErr := &GenericOpenAPIError{
			body:  localVarBody,
			error: err.Error(),
		}
		return localVarReturnValue, localVarHTTPResponse, newErr
	}

	return localVarReturnValue, localVarHTTPResponse, nil
}
