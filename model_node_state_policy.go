/*
plexsphere API

HTTP contract for plexsphere's meta endpoints (health, version, self-describing OpenAPI), the identity sign-in / token endpoints , and the admin IdP-binding management surface. The specification is the single source of truth — server stubs, clients, and documentation are generated from this file .  CSRF defence-in-depth (issue #181): every state-changing cookie-authenticated `/v1/_*` request (`POST`, `PATCH`, `PUT`, `DELETE`) is required to carry both an Origin / Sec-Fetch-Site signal AND echo the `plexsphere_csrf` cookie value in the `X-Plexsphere-CSRF` header. Violations surface as `403 application/problem+json` with `code` ∈ { `csrf-token-mismatch`, `csrf-origin-mismatch`, `csrf-origin-not-configured` }. Bearer-authenticated requests (`Authorization: Bearer …`) are exempt because the bearer scheme is not auto-attached by browsers; the sign-in surface (`/v1/auth/_*`) is exempt because it cannot carry a Principal yet.

API version: v1
*/

// Code generated by OpenAPI Generator (https://openapi-generator.tech); DO NOT EDIT.

package plexsphere

import (
	"encoding/json"
	"fmt"
)

// checks if the NodeStatePolicy type satisfies the MappedNullable interface at compile time
var _ MappedNullable = &NodeStatePolicy{}

// NodeStatePolicy Per-Node compiled policy fan-out block. Carries the merged rule list the controller projected for the addressed Node, the base64-encoded SHA-256 fingerprint that plexd uses to short-circuit reconcile when the locally-applied ruleset already matches, and the revision identifier of the policy-id-ascending first matched Policy. The block is `null` on the `NodeStateSnapshot` envelope when no compiled ruleset row exists for the addressed Node (the per-Node selector did not match any active Policy revision).  Multi-policy semantics: the controller persists one `plexsphere.policy_compiled_ruleset` row per (Node, Policy) tuple. When a Node matches multiple Policies, the snapshot composer merges those rows by sorting on `policy_id` and concatenating each row's `rule_payload` bytes in ascending order. `rules` decodes the merged byte stream into the canonical wire shape; `fingerprint` is `sha256.Sum256(payload_0 || payload_1 || ...)` over the same byte stream; `revision_id` is drawn from the policy-id-ascending first row so a single uuid remains on the wire. Plexd treats the fingerprint as an opaque comparison key — it is stable for a given (Node, set of matched Policy revisions, rule_payload byte streams) tuple and changes whenever any contributing row changes.
type NodeStatePolicy struct {
	// Identifier of the Policy revision the merged ruleset anchors on — drawn from the policy-id-ascending first row when the addressed Node matches multiple Policies. A Node matching a single Policy carries that Policy's revision id directly.
	RevisionId string `json:"revision_id"`
	// SHA-256 digest over the merged `rule_payload` byte stream the controller persisted in `plexsphere.policy_compiled_ruleset`. Exactly 32 bytes once decoded; on the wire the field is base64-encoded with standard padding (44 ASCII characters). For a Node matching a single Policy the digest equals `sha256.Sum256(rule_payload)`; for a Node matching multiple Policies it equals `sha256.Sum256(payload_0 || payload_1 || ...)` with `payload_i` ordered by ascending `policy_id`. The digest is an opaque comparison key — plexd compares it byte-for-byte against its locally-applied ruleset's stored digest and does not re-derive it from the `rules` array.
	Fingerprint string `json:"fingerprint"`
	// Canonical, deterministically-sorted L3/L4 rule list the addressed Node MUST program. For multi-policy matches the array is the per-Policy concatenation of each row's canonical rule list in policy-id-ascending order. Empty array is a legitimate state — it means the Node matches at least one Policy's selector pair but no Policy contributed any rules (or every rule was filtered out).
	Rules                []NodeStateCompiledRule `json:"rules"`
	AdditionalProperties map[string]interface{}
}

type _NodeStatePolicy NodeStatePolicy

// NewNodeStatePolicy instantiates a new NodeStatePolicy object
// This constructor will assign default values to properties that have it defined,
// and makes sure properties required by API are set, but the set of arguments
// will change when the set of required properties is changed
func NewNodeStatePolicy(revisionId string, fingerprint string, rules []NodeStateCompiledRule) *NodeStatePolicy {
	this := NodeStatePolicy{}
	this.RevisionId = revisionId
	this.Fingerprint = fingerprint
	this.Rules = rules
	return &this
}

// NewNodeStatePolicyWithDefaults instantiates a new NodeStatePolicy object
// This constructor will only assign default values to properties that have it defined,
// but it doesn't guarantee that properties required by API are set
func NewNodeStatePolicyWithDefaults() *NodeStatePolicy {
	this := NodeStatePolicy{}
	return &this
}

// GetRevisionId returns the RevisionId field value
func (o *NodeStatePolicy) GetRevisionId() string {
	if o == nil {
		var ret string
		return ret
	}

	return o.RevisionId
}

// GetRevisionIdOk returns a tuple with the RevisionId field value
// and a boolean to check if the value has been set.
func (o *NodeStatePolicy) GetRevisionIdOk() (*string, bool) {
	if o == nil {
		return nil, false
	}
	return &o.RevisionId, true
}

// SetRevisionId sets field value
func (o *NodeStatePolicy) SetRevisionId(v string) {
	o.RevisionId = v
}

// GetFingerprint returns the Fingerprint field value
func (o *NodeStatePolicy) GetFingerprint() string {
	if o == nil {
		var ret string
		return ret
	}

	return o.Fingerprint
}

// GetFingerprintOk returns a tuple with the Fingerprint field value
// and a boolean to check if the value has been set.
func (o *NodeStatePolicy) GetFingerprintOk() (*string, bool) {
	if o == nil {
		return nil, false
	}
	return &o.Fingerprint, true
}

// SetFingerprint sets field value
func (o *NodeStatePolicy) SetFingerprint(v string) {
	o.Fingerprint = v
}

// GetRules returns the Rules field value
func (o *NodeStatePolicy) GetRules() []NodeStateCompiledRule {
	if o == nil {
		var ret []NodeStateCompiledRule
		return ret
	}

	return o.Rules
}

// GetRulesOk returns a tuple with the Rules field value
// and a boolean to check if the value has been set.
func (o *NodeStatePolicy) GetRulesOk() ([]NodeStateCompiledRule, bool) {
	if o == nil {
		return nil, false
	}
	return o.Rules, true
}

// SetRules sets field value
func (o *NodeStatePolicy) SetRules(v []NodeStateCompiledRule) {
	o.Rules = v
}

func (o NodeStatePolicy) MarshalJSON() ([]byte, error) {
	toSerialize, err := o.ToMap()
	if err != nil {
		return []byte{}, err
	}
	return json.Marshal(toSerialize)
}

func (o NodeStatePolicy) ToMap() (map[string]interface{}, error) {
	toSerialize := map[string]interface{}{}
	toSerialize["revision_id"] = o.RevisionId
	toSerialize["fingerprint"] = o.Fingerprint
	toSerialize["rules"] = o.Rules

	for key, value := range o.AdditionalProperties {
		toSerialize[key] = value
	}

	return toSerialize, nil
}

func (o *NodeStatePolicy) UnmarshalJSON(data []byte) (err error) {
	// This validates that all required properties are included in the JSON object
	// by unmarshalling the object into a generic map with string keys and checking
	// that every required field exists as a key in the generic map.
	requiredProperties := []string{
		"revision_id",
		"fingerprint",
		"rules",
	}

	allProperties := make(map[string]interface{})

	err = json.Unmarshal(data, &allProperties)

	if err != nil {
		return err
	}

	for _, requiredProperty := range requiredProperties {
		if _, exists := allProperties[requiredProperty]; !exists {
			return fmt.Errorf("no value given for required property %v", requiredProperty)
		}
	}

	varNodeStatePolicy := _NodeStatePolicy{}

	err = json.Unmarshal(data, &varNodeStatePolicy)

	if err != nil {
		return err
	}

	*o = NodeStatePolicy(varNodeStatePolicy)

	additionalProperties := make(map[string]interface{})

	if err = json.Unmarshal(data, &additionalProperties); err == nil {
		delete(additionalProperties, "revision_id")
		delete(additionalProperties, "fingerprint")
		delete(additionalProperties, "rules")
		o.AdditionalProperties = additionalProperties
	}

	return err
}

type NullableNodeStatePolicy struct {
	value *NodeStatePolicy
	isSet bool
}

func (v NullableNodeStatePolicy) Get() *NodeStatePolicy {
	return v.value
}

func (v *NullableNodeStatePolicy) Set(val *NodeStatePolicy) {
	v.value = val
	v.isSet = true
}

func (v NullableNodeStatePolicy) IsSet() bool {
	return v.isSet
}

func (v *NullableNodeStatePolicy) Unset() {
	v.value = nil
	v.isSet = false
}

func NewNullableNodeStatePolicy(val *NodeStatePolicy) *NullableNodeStatePolicy {
	return &NullableNodeStatePolicy{value: val, isSet: true}
}

func (v NullableNodeStatePolicy) MarshalJSON() ([]byte, error) {
	return json.Marshal(v.value)
}

func (v *NullableNodeStatePolicy) UnmarshalJSON(src []byte) error {
	v.isSet = true
	return json.Unmarshal(src, &v.value)
}
